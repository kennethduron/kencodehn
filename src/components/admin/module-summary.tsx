import Link from "next/link";
import {Puzzle} from "lucide-react";
import type {ProjectAddOn} from "@/lib/add-ons/types";
import {commercialLabels,workLabels} from "@/lib/add-ons/types";
import {formatMinor} from "@/lib/billing/money";
export function ModuleSummary({items,title="Modulos y ampliaciones"}:{items:ProjectAddOn[];title?:string}){return <section className="kc-admin-card p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.16em] text-blue-700">Venta adicional</p><h2 className="mt-1 text-xl font-black">{title}</h2></div><Link href="/admin/modulos" className="inline-flex min-h-11 items-center font-black text-blue-700">Ver todos</Link></div><div className="mt-4 grid gap-3">{items.length?items.map(item=><Link key={item.id} href={`/admin/modulos/${item.id}`} className="grid gap-3 rounded-xl border p-4 sm:grid-cols-[auto_minmax(0,1fr)_auto] sm:items-center"><span className="grid h-10 w-10 place-items-center rounded-xl bg-blue-50 text-blue-700"><Puzzle size={19}/></span><div className="min-w-0"><strong className="block truncate">{item.name}</strong><span className="text-sm text-kc-muted">{commercialLabels[item.commercialStatus]} · {workLabels[item.workStatus]}</span></div><span className="font-black">{item.acceptedAmountMinor?formatMinor(item.acceptedAmountMinor,"USD"):"Por cotizar"}</span></Link>):<p className="rounded-xl border border-dashed p-5 text-center text-sm text-kc-muted">No hay ampliaciones asociadas.</p>}</div></section>}
