"use client";
import Link from "next/link";
import {useRouter} from "next/navigation";
import {ArrowLeft,CheckCircle2,Download,FileText,Plus,Save,Send,Trash2} from "lucide-react";
import {useMemo,useState} from "react";
import type {ProjectAddOn} from "@/lib/add-ons/types";
import {commercialLabels,proposalLabels,workLabels} from "@/lib/add-ons/types";
import {addMinor,formatMinor,formatSignedMinor,minorToDecimalInput,parseMoneyToMinor} from "@/lib/billing/money";
import {todayInHonduras} from "@/lib/time";

type Draft={label:string;amount:string;dueDate:string;dueTime:string;notes:string};
async function mutate(operation:string,payload:Record<string,unknown>){const response=await fetch("/api/admin/add-ons",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({operation,payload})});const body=await response.json();if(!response.ok)throw new Error(body.error||"No se pudo completar la operacion.");return body.result as Record<string,unknown>;}
export function ModuleDetail({addOn,canDraft,canManage,canApprove}:{addOn:ProjectAddOn;canDraft:boolean;canManage:boolean;canApprove:boolean}){
 const router=useRouter(),[tab,setTab]=useState<"summary"|"proposal"|"payments"|"work">("summary"),[saving,setSaving]=useState(false),[message,setMessage]=useState("");
 const draft=addOn.proposals.find(item=>item.status==="draft"),latest=addOn.proposals[0],sale=addOn.sale,plan=sale?addOn.paymentPlans.find(item=>item.status==="draft")??addOn.paymentPlans[0]:null;
 const[rows,setRows]=useState<Draft[]>(plan?.installments.map(item=>({label:item.label,amount:minorToDecimalInput(item.amountMinor),dueDate:item.dueDate,dueTime:item.dueTime||"",notes:item.notes}))??[{label:"Pago unico",amount:addOn.acceptedAmountMinor?minorToDecimalInput(addOn.acceptedAmountMinor):"0.00",dueDate:"",dueTime:"",notes:""}]);
 const total=useMemo(()=>{try{return addMinor(rows.map(item=>parseMoneyToMinor(item.amount)));}catch{return BigInt(-1);}},[rows]),accepted=BigInt(addOn.acceptedAmountMinor??"0"),difference=accepted-total;
 async function run(operation:string,payload:Record<string,unknown>,success:string){setSaving(true);setMessage("");try{await mutate(operation,payload);setMessage(success);router.refresh();}catch(error){setMessage(error instanceof Error?error.message:"No se pudo completar.");}finally{setSaving(false);}}
 async function saveProposal(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);const payload={...(draft?{proposalId:draft.id}:{addOnId:addOn.id}),title:String(data.get("title")),scope:String(data.get("scope")),amountMinor:parseMoneyToMinor(String(data.get("amount"))).toString(),currency:"USD",paymentTerms:String(data.get("paymentTerms")),monthlyAddOnMinor:parseMoneyToMinor(String(data.get("monthly"))).toString(),estimatedDelivery:String(data.get("delivery")),validUntil:String(data.get("validUntil")),clientNotes:String(data.get("clientNotes")),internalNotes:String(data.get("internalNotes"))};await run(draft?"proposal_update":"proposal_create",payload,draft?"Borrador actualizado.":"Nueva version de propuesta creada.");}
 async function savePlan(){if(!sale)return;await run("payment_plan_save",{saleId:sale.id,planId:plan?.status==="draft"?plan.id:"",name:"Plan personalizado",currency:"USD",installments:rows.map((row,index)=>({sequence:index+1,label:row.label,amountMinor:parseMoneyToMinor(row.amount).toString(),currency:"USD",dueDate:row.dueDate,dueTime:row.dueTime,notes:row.notes}))},"Plan de pago guardado sin registrar pagos.");}
 return <div className="grid gap-5">
  <div><Link href="/admin/modulos" className="inline-flex min-h-11 items-center gap-2 font-black text-blue-700"><ArrowLeft size={17}/> Volver a modulos</Link><div className="mt-2 flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-black uppercase tracking-[.18em] text-blue-700">{addOn.clientName} · <Link className="underline" href={`/admin/proyectos/${addOn.projectId}`}>{addOn.projectName}</Link></p><h1 className="mt-1 font-display text-3xl font-black">{addOn.name}</h1><p className="mt-2 text-sm text-kc-muted">Ampliacion independiente · USD</p></div><div className="flex flex-wrap gap-2"><span className="rounded-full border bg-blue-50 px-3 py-1 text-sm font-black text-blue-700">{commercialLabels[addOn.commercialStatus]}</span><span className="rounded-full border bg-slate-50 px-3 py-1 text-sm font-black">{workLabels[addOn.workStatus]}</span></div></div></div>
  <div className="kc-scroll-tabs overflow-x-auto"><div role="tablist" className="inline-flex min-w-max gap-1 rounded-2xl border bg-white p-1.5">{[["summary","Resumen"],["proposal","Propuesta"],["payments","Pagos"],["work","Desarrollo"]].map(([value,label])=><button key={value} role="tab" aria-selected={tab===value} onClick={()=>setTab(value as typeof tab)} className={`min-h-11 rounded-xl px-4 font-black ${tab===value?"bg-blue-600 text-white":"text-slate-600"}`}>{label}</button>)}</div></div>
  {message?<p role="status" className="rounded-xl border border-blue-200 bg-blue-50 p-3 font-bold text-blue-800">{message}</p>:null}
  {tab==="summary"?<div className="grid gap-4 lg:grid-cols-[minmax(0,1fr)_22rem]"><section className="kc-admin-card p-5"><h2 className="text-xl font-black">Resumen comercial</h2><p className="mt-3 whitespace-pre-wrap text-sm leading-6 text-kc-muted">{addOn.description||"Sin descripcion."}</p><dl className="mt-5 grid gap-4 sm:grid-cols-3"><div><dt className="text-xs font-black uppercase text-kc-muted">Proyecto original</dt><dd className="mt-1 font-black">Se conserva sin cambios</dd></div><div><dt className="text-xs font-black uppercase text-kc-muted">Propuesta vigente</dt><dd className="mt-1 font-black">{latest?`${latest.proposalNumber} · v${latest.version}`:"Pendiente"}</dd></div><div><dt className="text-xs font-black uppercase text-kc-muted">Venta adicional</dt><dd className="mt-1 font-black">{addOn.acceptedAmountMinor?formatMinor(addOn.acceptedAmountMinor,"USD"):"Sin aprobar"}</dd></div></dl></section><aside className="kc-admin-card p-5"><h2 className="text-xl font-black">Control financiero</h2><dl className="mt-4 grid gap-3 text-sm"><div className="flex justify-between"><dt>Pagado</dt><dd className="font-black text-emerald-700">{formatMinor(addOn.paidMinor,"USD")}</dd></div><div className="flex justify-between"><dt>Pendiente</dt><dd className="font-black text-amber-700">{formatMinor(addOn.outstandingMinor,"USD")}</dd></div><div className="flex justify-between"><dt>Cargo mensual</dt><dd className="font-black">{addOn.recurring?formatMinor(addOn.recurring.monthlyAmountMinor,"USD"):"No configurado"}</dd></div></dl></aside></div>:null}
  {tab==="proposal"?<div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_22rem]"><form onSubmit={saveProposal} className="kc-admin-card grid gap-4 p-5"><div><p className="text-xs font-black uppercase tracking-[.16em] text-blue-700">Documento comercial</p><h2 className="mt-1 text-2xl font-black">{draft?`Editar borrador v${draft.version}`:"Crear nueva version"}</h2></div><label className="grid gap-2 text-sm font-bold">Titulo<input name="title" defaultValue={draft?.title||latest?.title||addOn.name} disabled={!canDraft} required minLength={2} className="min-h-11 rounded-xl border px-3"/></label><label className="grid gap-2 text-sm font-bold">Alcance para el cliente<textarea name="scope" defaultValue={draft?.scope||""} disabled={!canDraft} required rows={7} className="rounded-xl border p-3"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Implementacion (USD)<input name="amount" type="number" min="0.01" step="0.01" defaultValue={draft?minorToDecimalInput(draft.amountMinor):"0.00"} disabled={!canDraft} required className="min-h-11 rounded-xl border px-3"/></label><label className="grid gap-2 text-sm font-bold">Cargo mensual adicional (USD)<input name="monthly" type="number" min="0" step="0.01" defaultValue={draft?minorToDecimalInput(draft.monthlyAddOnMinor):"0.00"} disabled={!canDraft} required className="min-h-11 rounded-xl border px-3"/></label></div><label className="grid gap-2 text-sm font-bold">Forma de pago<textarea name="paymentTerms" defaultValue={draft?.paymentTerms||""} disabled={!canDraft} rows={3} className="rounded-xl border p-3"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Entrega estimada<input name="delivery" defaultValue={draft?.estimatedDelivery||""} disabled={!canDraft} className="min-h-11 rounded-xl border px-3"/></label><label className="grid gap-2 text-sm font-bold">Valida hasta<input name="validUntil" type="date" defaultValue={draft?.validUntil||""} disabled={!canDraft} className="min-h-11 rounded-xl border px-3"/></label></div><label className="grid gap-2 text-sm font-bold">Notas para el cliente<textarea name="clientNotes" defaultValue={draft?.clientNotes||""} disabled={!canDraft} rows={3} className="rounded-xl border p-3"/></label><label className="grid gap-2 text-sm font-bold">Notas internas (nunca aparecen en PDF)<textarea name="internalNotes" defaultValue={draft?.internalNotes||""} disabled={!canDraft} rows={3} className="rounded-xl border p-3"/></label>{canDraft?<button disabled={saving} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-blue-600 font-black text-white"><Save size={17}/> Guardar propuesta</button>:null}</form><aside className="grid content-start gap-3">{addOn.proposals.map(proposal=><article key={proposal.id} className="kc-admin-card p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-black">{proposal.proposalNumber} · v{proposal.version}</h3><p className="text-sm text-kc-muted">{proposalLabels[proposal.status]} · {formatMinor(proposal.amountMinor,"USD")}</p></div><FileText className="text-blue-600" size={20}/></div>{canManage?<a href={`/api/admin/add-ons/proposals/${proposal.id}/pdf?module=${addOn.id}`} className="mt-3 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl border font-black text-blue-700"><Download size={17}/> Descargar PDF</a>:null}{proposal.status==="draft"&&canManage?<button disabled={saving} onClick={()=>run("proposal_mark_sent",{proposalId:proposal.id},"Propuesta marcada como enviada; no se envio ningun email.")} className="mt-2 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl border font-black"><Send size={17}/> Marcar enviada</button>:null}{proposal.status==="sent"&&canApprove?<div className="mt-2 grid grid-cols-2 gap-2"><button onClick={()=>run("proposal_accept",{proposalId:proposal.id,effectiveDate:todayInHonduras(),decisionNotes:"Aprobada por Owner/Admin"},"Propuesta aceptada y venta adicional registrada.")} className="min-h-11 rounded-xl bg-emerald-700 font-black text-white">Aceptar</button><button onClick={()=>run("proposal_reject",{proposalId:proposal.id,decisionNotes:"Propuesta rechazada"},"Propuesta rechazada sin obligaciones financieras.")} className="min-h-11 rounded-xl border border-rose-300 font-black text-rose-700">Rechazar</button></div>:null}</article>)}</aside></div>:null}
  {tab==="payments"?<div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_22rem]">{sale?<section className="kc-admin-card p-5"><h2 className="text-2xl font-black">Plan de pago personalizado</h2><p className="mt-1 text-sm text-kc-muted">Debe sumar exactamente {formatMinor(sale.acceptedAmountMinor,"USD")}. Guardar no registra pagos; activar crea cuentas por cobrar.</p><div className="mt-5 grid gap-3">{rows.map((row,index)=><article key={index} className="rounded-2xl border bg-slate-50 p-4"><div className="flex justify-between"><strong>Cuota {index+1}</strong>{canManage&&rows.length>1?<button aria-label={`Eliminar cuota ${index+1}`} onClick={()=>setRows(current=>current.filter((_,i)=>i!==index))} className="grid h-11 w-11 place-items-center rounded-xl border text-rose-700"><Trash2 size={17}/></button>:null}</div><div className="mt-3 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><input aria-label="Concepto" value={row.label} onChange={e=>setRows(current=>current.map((x,i)=>i===index?{...x,label:e.target.value}:x))} className="min-h-11 rounded-xl border px-3"/><input aria-label="Monto USD" value={row.amount} type="number" min="0.01" step="0.01" onChange={e=>setRows(current=>current.map((x,i)=>i===index?{...x,amount:e.target.value}:x))} className="min-h-11 rounded-xl border px-3"/><input aria-label="Vencimiento" value={row.dueDate} type="date" onChange={e=>setRows(current=>current.map((x,i)=>i===index?{...x,dueDate:e.target.value}:x))} className="min-h-11 rounded-xl border px-3"/><input aria-label="Hora" value={row.dueTime} type="time" onChange={e=>setRows(current=>current.map((x,i)=>i===index?{...x,dueTime:e.target.value}:x))} className="min-h-11 rounded-xl border px-3"/></div></article>)}</div>{canManage?<div className="mt-4 grid gap-2 sm:grid-cols-3"><button onClick={()=>setRows(current=>[...current,{label:`Cuota ${current.length+1}`,amount:"0.00",dueDate:"",dueTime:"",notes:""}])} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl border font-black"><Plus size={17}/> Agregar</button><button onClick={savePlan} disabled={saving||difference!==BigInt(0)} className="min-h-11 rounded-xl border border-blue-600 font-black text-blue-700 disabled:opacity-40">Guardar plan</button><button onClick={()=>plan&&run("payment_plan_activate",{planId:plan.id},"Plan activado y obligaciones generadas.")} disabled={saving||plan?.status!=="draft"||difference!==BigInt(0)} className="min-h-11 rounded-xl bg-blue-600 font-black text-white disabled:opacity-40">Activar</button></div>:null}</section>:<div className="kc-admin-card p-8 text-center"><h2 className="text-xl font-black">Aun no hay venta aprobada</h2><p className="mt-2 text-kc-muted">El plan se habilita despues de aceptar una propuesta.</p></div>}<aside className="kc-admin-card p-5"><h2 className="text-xl font-black">Validacion</h2><dl className="mt-4 grid gap-3 text-sm"><div className="flex justify-between"><dt>Aprobado</dt><dd className="font-black">{formatMinor(accepted,"USD")}</dd></div><div className="flex justify-between"><dt>Distribuido</dt><dd className="font-black">{total>=BigInt(0)?formatMinor(total,"USD"):"Invalido"}</dd></div><div className="flex justify-between"><dt>Diferencia</dt><dd className={difference===BigInt(0)?"font-black text-emerald-700":"font-black text-rose-700"}>{total>=BigInt(0)?formatSignedMinor(difference,"USD"):"Invalido"}</dd></div></dl></aside></div>:null}
  {tab==="work"?<div className="grid gap-5 lg:grid-cols-2"><form onSubmit={e=>{e.preventDefault();const data=new FormData(e.currentTarget);void run("work_status_update",{addOnId:addOn.id,status:String(data.get("status")),plannedStartDate:String(data.get("start")),targetDeliveryDate:String(data.get("target")),actualDeliveryDate:String(data.get("actual")),notes:String(data.get("notes"))},"Estado de desarrollo actualizado.");}} className="kc-admin-card grid gap-4 p-5"><h2 className="text-2xl font-black">Ejecucion y entrega</h2><label className="grid gap-2 text-sm font-bold">Estado<select name="status" defaultValue={addOn.workStatus} disabled={!canManage} className="min-h-11 rounded-xl border px-3">{Object.entries(workLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><div className="grid gap-4 sm:grid-cols-3"><label className="grid gap-2 text-sm font-bold">Inicio<input name="start" type="date" defaultValue={addOn.plannedStartDate||""} className="min-h-11 rounded-xl border px-3"/></label><label className="grid gap-2 text-sm font-bold">Objetivo<input name="target" type="date" defaultValue={addOn.targetDeliveryDate||""} className="min-h-11 rounded-xl border px-3"/></label><label className="grid gap-2 text-sm font-bold">Entrega real<input name="actual" type="date" defaultValue={addOn.actualDeliveryDate||""} className="min-h-11 rounded-xl border px-3"/></label></div><label className="grid gap-2 text-sm font-bold">Notas de entrega<textarea name="notes" defaultValue={addOn.deliveryNotes} rows={4} className="rounded-xl border p-3"/></label>{canManage?<button className="min-h-11 rounded-xl bg-blue-600 font-black text-white">Guardar estado</button>:null}</form>{sale?<form onSubmit={e=>{e.preventDefault();const data=new FormData(e.currentTarget);void run("recurring_configure",{saleId:sale.id,id:addOn.recurring?.id||"",name:String(data.get("name")),monthlyAmountMinor:parseMoneyToMinor(String(data.get("amount"))).toString(),currency:"USD",startDate:String(data.get("startDate")),billingDay:Number(data.get("day")),billingTime:String(data.get("time")),status:String(data.get("status"))},"Cargo mensual adicional configurado.");}} className="kc-admin-card grid gap-4 p-5"><h2 className="text-2xl font-black">Cargo mensual adicional</h2><p className="text-sm text-kc-muted">Separado del servicio recurrente base y del precio de implementacion.</p><label className="grid gap-2 text-sm font-bold">Concepto<input name="name" defaultValue={addOn.recurring?.name||`${addOn.name} mensual`} required className="min-h-11 rounded-xl border px-3"/></label><label className="grid gap-2 text-sm font-bold">Monto mensual USD<input name="amount" type="number" min="0.01" step="0.01" defaultValue={addOn.recurring?minorToDecimalInput(addOn.recurring.monthlyAmountMinor):latest?minorToDecimalInput(latest.monthlyAddOnMinor):"0.00"} required className="min-h-11 rounded-xl border px-3"/></label><div className="grid gap-3 sm:grid-cols-3"><input aria-label="Inicio mensualidad" name="startDate" type="date" defaultValue={addOn.recurring?.startDate||todayInHonduras()} required className="min-h-11 rounded-xl border px-3"/><input aria-label="Dia de cobro" name="day" type="number" min="1" max="28" defaultValue={addOn.recurring?.billingDay||1} required className="min-h-11 rounded-xl border px-3"/><input aria-label="Hora de cobro" name="time" type="time" defaultValue={addOn.recurring?.billingTime||"09:00"} required className="min-h-11 rounded-xl border px-3"/></div><select name="status" aria-label="Estado de mensualidad" defaultValue={addOn.recurring?.status||"draft"} className="min-h-11 rounded-xl border px-3"><option value="draft">Borrador</option><option value="active">Activo</option><option value="paused">Pausado</option><option value="cancelled">Cancelado</option></select>{canManage?<button className="min-h-11 rounded-xl bg-blue-600 font-black text-white">Guardar mensualidad</button>:null}</form>:null}</div>:null}
 </div>;
}


