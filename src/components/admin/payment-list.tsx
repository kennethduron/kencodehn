import Link from "next/link";
import { WalletCards } from "lucide-react";
import { formatMinor } from "@/lib/billing/money";
import type { BillingPayment } from "@/lib/billing/types";

const labels:Record<string,string>={bank_transfer:"Transferencia bancaria",cash:"Efectivo",card:"Tarjeta",paypal:"PayPal",other:"Otro"};
export function PaymentList({items,total,page,pageSize}:{items:BillingPayment[];total:number;page:number;pageSize:number}){return <div className="grid gap-4"><form method="get" className="kc-filter-bar kc-admin-card flex flex-wrap gap-3 p-3"><select name="status" aria-label="Filtrar estado" className="min-h-11 rounded-xl border px-3 text-sm"><option value="">Todos los estados</option><option value="posted">Registrados</option><option value="reversed">Revertidos</option></select><button className="min-h-11 rounded-xl border border-blue-200 bg-blue-50 px-4 text-sm font-black text-blue-700">Aplicar</button></form>{items.length?<><div className="kc-mobile-cards grid gap-3 md:hidden">{items.map(item=><Link key={item.id} href={`/admin/pagos/${item.id}`} className="kc-admin-card p-4"><div className="flex justify-between gap-3"><div><p className="font-black">{item.clientName}</p><p className="mt-1 text-sm text-kc-muted">{labels[item.method]||item.method} · {new Date(item.paidAt).toLocaleDateString("es-HN")}</p></div><strong>{formatMinor(item.amountMinor,item.currency)}</strong></div><p className="mt-3 text-xs font-bold text-kc-muted">{item.allocations.length} aplicaciones · {item.status==="posted"?"Registrado":"Revertido"}</p></Link>)}</div><div className="kc-responsive-table hidden overflow-x-auto rounded-2xl border bg-white md:block"><table className="w-full min-w-[780px] text-left text-sm"><thead><tr>{["Fecha","Cliente","Metodo","Referencia","Aplicaciones","Importe","Estado",""].map(label=><th key={label} className="px-4 py-3 text-xs font-black uppercase tracking-[.08em] text-kc-muted">{label}</th>)}</tr></thead><tbody>{items.map(item=><tr key={item.id} className="border-t"><td className="px-4 py-3">{new Date(item.paidAt).toLocaleString("es-HN")}</td><td className="px-4 py-3 font-bold">{item.clientName}</td><td className="px-4 py-3">{labels[item.method]||item.method}</td><td className="px-4 py-3">{item.reference||"—"}</td><td className="px-4 py-3">{item.allocations.length}</td><td className="px-4 py-3 font-black">{formatMinor(item.amountMinor,item.currency)}</td><td className="px-4 py-3">{item.status==="posted"?"Registrado":"Revertido"}</td><td className="px-4 py-3"><Link href={`/admin/pagos/${item.id}`} className="font-black text-blue-700">Detalle</Link></td></tr>)}</tbody></table></div></>:<div className="kc-admin-card grid min-h-64 place-items-center p-7 text-center"><div><WalletCards className="mx-auto text-blue-700" size={34}/><h2 className="mt-4 text-xl font-black">No hay pagos registrados</h2><p className="mt-2 text-sm text-kc-muted">Los pagos reales apareceran aqui. No hay datos ficticios.</p></div></div>}<div className="flex items-center justify-between"><p className="text-sm text-kc-muted">{total} pagos</p><div className="flex gap-2"><Link aria-disabled={page<=1} href={`?page=${Math.max(1,page-1)}`} className="inline-flex min-h-11 items-center rounded-xl border px-4 text-sm font-black aria-disabled:pointer-events-none aria-disabled:opacity-40">Anterior</Link><Link aria-disabled={page*pageSize>=total} href={`?page=${page+1}`} className="inline-flex min-h-11 items-center rounded-xl border px-4 text-sm font-black aria-disabled:pointer-events-none aria-disabled:opacity-40">Siguiente</Link></div></div></div>}
