import Link from "next/link";
import { ArrowDownRight, ArrowRight, ArrowUpRight, Banknote, CircleDollarSign, ReceiptText, Scale, TriangleAlert } from "lucide-react";
import { formatMinor,formatSignedMinor } from "@/lib/billing/money";
import type { FinanceReportRow,FinanceSeriesPoint,FinanceSummary } from "@/lib/finance/types";
import { financePeriodLabels } from "@/lib/finance/range";
import { FinanceEmptyState,FinanceNav,FinancePageHeader } from "./finance-nav";

const metrics=[
  ["Valor vendido","soldMinor",CircleDollarSign,"Importe contractual registrado; no es efectivo cobrado."],
  ["Cobrado","collectedMinor",Banknote,"Dinero real recibido durante el periodo."],
  ["Por cobrar","outstandingMinor",ArrowUpRight,"Saldo de obligaciones del periodo."],
  ["Vencido","overdueMinor",TriangleAlert,"Saldo que supero su fecha de cobro."],
  ["Gastos","expenseMinor",ReceiptText,"Salidas de caja registradas y vigentes."],
  ["Resultado neto de caja","netCashMinor",Scale,"Cobrado menos gastos; no es utilidad contable."],
] as const;

function FinanceBars({points,currency}:{points:FinanceSeriesPoint[];currency:string}){const max=points.reduce((value,item)=>{const a=BigInt(item.collectedMinor),b=BigInt(item.expenseMinor);return a>value?a:b>value?b:value;},BigInt(1));return <article className="kc-admin-card p-4 sm:p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-black text-kc-text">Cobrado vs. gastado</h2><p className="mt-1 text-sm text-kc-muted">Serie mensual · {currency}</p></div><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-black text-blue-700">{currency}</span></div><div className="mt-5 grid gap-4" role="img" aria-label={`Comparacion mensual de cobros y gastos en ${currency}`}>{points.map((item)=>{const collected=BigInt(item.collectedMinor),spent=BigInt(item.expenseMinor);const cw=Number(collected*BigInt(100)/max),sw=Number(spent*BigInt(100)/max);return <div key={item.monthStart} className="grid grid-cols-[4.8rem_minmax(0,1fr)] items-center gap-3"><span className="text-xs font-bold text-kc-muted">{new Intl.DateTimeFormat("es-HN",{month:"short",year:"2-digit",timeZone:"UTC"}).format(new Date(`${item.monthStart}T12:00:00Z`))}</span><div className="grid gap-1.5"><span className="h-2 rounded-full bg-blue-100"><span className="block h-full rounded-full bg-blue-600" style={{width:`${cw}%`}} title={`Cobrado ${formatMinor(collected,currency)}`}/></span><span className="h-2 rounded-full bg-orange-100"><span className="block h-full rounded-full bg-orange-500" style={{width:`${sw}%`}} title={`Gastado ${formatMinor(spent,currency)}`}/></span></div></div>})}</div><div className="mt-4 flex gap-4 text-xs font-bold text-kc-muted"><span className="flex items-center gap-1.5"><i className="h-2.5 w-2.5 rounded-full bg-blue-600"/>Cobrado</span><span className="flex items-center gap-1.5"><i className="h-2.5 w-2.5 rounded-full bg-orange-500"/>Gastado</span></div><details className="mt-4 text-sm"><summary className="font-bold text-blue-700">Ver alternativa tabular</summary><div className="mt-2 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr><th className="p-2">Mes</th><th className="p-2">Cobrado</th><th className="p-2">Gastado</th></tr></thead><tbody>{points.map(item=><tr key={item.monthStart} className="border-t"><td className="p-2">{item.monthStart}</td><td className="p-2">{formatMinor(item.collectedMinor,currency)}</td><td className="p-2">{formatMinor(item.expenseMinor,currency)}</td></tr>)}</tbody></table></div></details></article>}

export function FinanceDashboard({summary,series,movements,range}:{summary:FinanceSummary[];series:Record<string,FinanceSeriesPoint[]>;movements:FinanceReportRow[];range:{period:string;from:string;to:string}}){return <div className="grid gap-5"><FinancePageHeader title="Resumen financiero" description="Efectivo, obligaciones, ventas y gastos separados con precision por moneda." action={<Link href="/admin/finanzas/gastos" className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-black text-white">Registrar gasto <ArrowRight size={16}/></Link>}/><FinanceNav active="summary"/><form className="kc-filter-bar kc-admin-card flex flex-wrap items-end gap-3 p-3" method="get"><label className="grid gap-1 text-xs font-bold text-kc-muted">Periodo<select name="period" defaultValue={range.period} className="min-h-11 rounded-xl border px-3 text-sm">{Object.entries(financePeriodLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label className="grid gap-1 text-xs font-bold text-kc-muted">Desde<input name="from" type="date" defaultValue={range.from} className="min-h-11 rounded-xl border px-3 text-sm"/></label><label className="grid gap-1 text-xs font-bold text-kc-muted">Hasta<input name="to" type="date" defaultValue={range.to} className="min-h-11 rounded-xl border px-3 text-sm"/></label><button className="min-h-11 rounded-xl border border-blue-200 bg-blue-50 px-4 text-sm font-black text-blue-700">Actualizar</button><p className="ml-auto pb-2 text-xs font-bold text-kc-muted">Zona horaria: America/Tegucigalpa</p></form>{summary.length===0?<FinanceEmptyState/>:summary.map(group=><section key={group.currency} className="grid gap-4" aria-labelledby={`currency-${group.currency}`}><div className="flex items-center gap-3"><h2 id={`currency-${group.currency}`} className="text-lg font-black text-kc-text">Resumen {group.currency}</h2><span className="rounded-full border bg-white px-2.5 py-1 text-xs font-black text-blue-700">Sin conversion de moneda</span></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-6">{metrics.map(([label,key,Icon,detail])=>{const raw=BigInt(group[key]);const signed=key==="netCashMinor";const negative=signed&&raw<BigInt(0);return <article key={key} className="kc-admin-card p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold text-kc-muted">{label}</p><p className={`mt-2 text-xl font-black ${negative?"text-rose-700":"text-kc-text"}`}>{signed?formatSignedMinor(raw,group.currency):formatMinor(raw,group.currency)}</p></div><span className={`grid h-9 w-9 place-items-center rounded-xl ${key==="expenseMinor"||negative?"bg-orange-50 text-orange-700":"bg-blue-50 text-blue-700"}`}><Icon size={18}/></span></div><p className="mt-3 text-xs leading-5 text-kc-muted">{detail}</p></article>})}</div>{series[group.currency]?<FinanceBars points={series[group.currency]} currency={group.currency}/>:null}</section>)}<section className="kc-admin-card p-4 sm:p-5"><div className="flex items-center justify-between gap-3"><div><h2 className="text-lg font-black text-kc-text">Movimientos recientes</h2><p className="mt-1 text-sm text-kc-muted">Pagos recibidos y gastos, sin mezclar sus tablas de origen.</p></div><Link href="/admin/finanzas/movimientos" className="text-sm font-black text-blue-700">Ver todos</Link></div>{movements.length?<div className="mt-4 grid gap-2">{movements.slice(0,6).map(item=><div key={`${item.recordType}-${item.recordId}`} className="flex items-center gap-3 rounded-xl border p-3"><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-xl ${item.recordType==="expense"?"bg-orange-50 text-orange-700":"bg-emerald-50 text-emerald-700"}`}>{item.recordType==="expense"?<ArrowDownRight size={18}/>:<ArrowUpRight size={18}/>}</span><span className="min-w-0 flex-1"><strong className="block truncate text-sm text-kc-text">{item.concept}</strong><span className="block truncate text-xs text-kc-muted">{item.party} · {item.occurredOn}</span></span><strong className={BigInt(item.amountMinor)<BigInt(0)?"text-rose-700":"text-emerald-700"}>{formatSignedMinor(BigInt(item.amountMinor),item.currency)}</strong></div>)}</div>:<p className="mt-4 rounded-xl border border-dashed p-5 text-sm text-kc-muted">Sin movimientos en el periodo seleccionado.</p>}</section></div>}
